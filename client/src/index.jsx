import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App.jsx';

// Attach App to the DOM, replacing the div with ID InfoRoot
ReactDOM.render(<App/>, document.getElementById('InfoRoot'));